[package]
name = "sprout"
version = "0.1.5"
edition = "2024"
description = "git worktrees with CoW-cloned working state (macOS/APFS)"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
ignore = "0.4"
libc = "0.2"
serde_json = "1"

[profile.release]
lto = true
strip = true
